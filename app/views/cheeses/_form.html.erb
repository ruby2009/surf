<%= simple_form_for @cheese do |f| %>

  <%= f.input :name %>
  <%= f.input :description, as: :text %>
  <%= f.input :picture %>
  <%= f.input :cheese_photo %>
  <%= f.button :submit, class: "btn btn-primary"%>

<% end %>
